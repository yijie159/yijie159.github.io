---
title: 用户签到
date: 2026-06-18 14:16:08
tags:
  - 黑马点评
categories:
  - 黑马点评
---
# BitMap用法<!--more-->

`tb_sign`表

![image-20260323143818422](/images/黑马点评/image-20260323143818422.png)

假如有1000w用户，平均每人每年签到次数为10次，则这张表一年的数据量为1亿条

每签到一次需要使用(8+8+1+1+3+1)共22字节的内存，一个月则最多需要600多字节

我们按照月来统计用户签到信息，签到记录为1，未签到则记录为0

把每个bit位对应当月的每一天，形成了映射关系。用0和1标示业务状态，这种思路就称为位图(BitMap)。Redis中是利用string类型数据结构实现BitMap，因此最大上限是512M，转换为bit则是2^32个bit位。

BitMap的操作命令有：

`SETBIT`：向指定位置(offset)存入一个0或1

- `SETBIT key offset value`
  - ![image-20260323145938659](/images/黑马点评/image-20260323145938659.png)
  - 对应11100111

`GETBIT`：获取指定位置(offset)的bit值

- `GETBIT key offset`
  - ![image-20260323150041971](/images/黑马点评/image-20260323150041971.png)

`BITCOUNT`：统计BitMap中值为1的bit位的数量

- ![image-20260323150057783](/images/黑马点评/image-20260323150057783.png)

`BITFIELD`：操作(查询、修改、自增)BitMap中bit数组中的指定位置(offset)的值

- `BITFIELD key [GET type offset] [SET type offset value] [INCR type offset increment] [OVERFLOW WRAP|SAT|FAIL]`![image-20260323150356971](/images/黑马点评/image-20260323150356971.png)
- u是无符号，i是有符号。u2是11，u3是111，u4是1110

`BITFIELD_RO`：获取BitMap中bit数组，并以十进制形式返回

`BITOP`：将多个BitMap的结果做位运算(与、或、异或)

`BITPOS`：查找bit数组中指定范围内**第一个0或1出现**的位置

- ![image-20260323151023147](/images/黑马点评/image-20260323151023147.png)

# 签到功能

需求：实现签到接口，将当前用户当天签到信息保存到Redis中

|          | 说明       |
| -------- | ---------- |
| 请求方式 | Post       |
| 请求路径 | /user/sign |
| 请求参数 | 无         |
| 返回值   | 无         |

提示：因为BitMap底层是基于String数据结构，因此其操作也都封装在字符串相关操作中了![image-20260323151347357](/images/黑马点评/image-20260323151347357.png)

UserController.java

```java
/**
 * 签到
 * @return
 */
@PostMapping("/sign")
public Result sign(){
    return userService.sign();
}
```

UserServiceImpl.java

```java
    /**
     * 签到功能
     * @return
     */
    @Override
    public Result sign() {
        // 1.获取当前登录用户
        Long userId = UserHolder.getUser().getId();

        // 2.获取日期
        LocalDateTime now = LocalDateTime.now();

        // 3.拼接key
        String keySuffix = now.format(DateTimeFormatter.ofPattern(":yyyyMM"));
        String key = USER_SIGN_KEY + userId + keySuffix;

        // 4.获取今天是本月的第几天
        int dayOfMonth = now.getDayOfMonth();

        // 5.写入Redis SETBIT key offset 1
        stringRedisTemplate.opsForValue().setBit(key,dayOfMonth-1,true);

        return Result.ok();
    }
```

思路：

- 获取当前登录用户
- 获取日期
- 拼接key
  - 使用`now.format(DateTimeFormatter.ofPattern(":yyyyMM"));`定义日期的形式
- 获取今天是本月的第几天
  - `now.getDayOfMonth();`方法获取这是这个月的第几天
- 写入Redis
  - `stringRedisTemplate.opsForValue().setBit(key,dayOfMonth-1,true);`由于BitMap已经整合进了String类型中，所以依旧使用`opsForValue()`使用`setBit`来使用`SETBIT`：向指定位置(offset)存入一个0或1

# 签到统计

问题1：什么叫做连续签到天数

从最后一次签到开始向前统计，直到遇到第一次未签到为止，计算总的签到次数，就是连续签到天数![image-20260323185738528](/images/黑马点评/image-20260323185738528.png)

问题2：如何得到本月到今天为止的所有签到数据？

`BITFIELD key GET u[dayOfMonth] 0`

问题3：如何从后向前遍历每个bit位？

与1做与运算，就能得到最后一个bit位

随后右移1位，下一个bit位就成为了最后一个bit位

需求：实现下面接口，统计当前用户截至当前时间在本月的连续签到天数

|          | 说明             |
| -------- | ---------------- |
| 请求方式 | GET              |
| 请求路径 | /user/sign/count |
| 请求参数 | 无               |
| 返回值   | 连续签到天数     |

UserController.java

```java
    /**
     * 签到统计
     * @return
     */
    @GetMapping("/sign/count")
    public Result signCount(){
        return userService.signCount();
    }
```

UserServiceImpl.java

```java
    /**
     * 签到功能
     * @return
     */
    @Override
    public Result signCount() {
        // 1.获取当前登录用户
        Long userId = UserHolder.getUser().getId();

        // 2.获取日期
        LocalDateTime now = LocalDateTime.now();

        // 3.拼接key
        String keySuffix = now.format(DateTimeFormatter.ofPattern(":yyyyMM"));
        String key = USER_SIGN_KEY + userId + keySuffix;

        // 4.获取今天是本月的第几天
        int dayOfMonth = now.getDayOfMonth();

        // 5.获取本月截止今天为止的所有的签到记录，返回的是一个十进制的数字 BITFIELD sign:5:202203 GET u14 0
        List<Long> result = stringRedisTemplate.opsForValue().bitField(key,
                BitFieldSubCommands.create().
                        get(BitFieldSubCommands.BitFieldType.unsigned(dayOfMonth)).valueAt(0)
        );

        if (result == null || result.size() == 0)
        {
            // 未签到
            return Result.ok();
        }
        Long num = result.get(0);
        if (num == null && num == 0L){
            return Result.ok(0);
        }
        // 6.循环遍历
        int count = 0;
        while (true){
            // 6.1.让这个数字与1做与运算，得到数字的最后一个bit位
            if ((num & 1) == 0) {
                // 如果为0，说明未签到，结束
                break;
            }else {
                //如果不为0，说明已签到，计数器+1
                count++;
            }
            //把数字右移一位，抛弃最后一个bit位，继续下一个bit位
            num >>>= 1;
        }

        return Result.ok(count);
    }
```

思路如下(前面4步与前一样)：

- 获取本月截止今天为止的所有的签到记录，返回的是一个十进制的数字
  - `stringRedisTemplate.opsForValue().bitField(key,BitFieldSubCommands.create().get(BitFieldSubCommands.BitFieldType.unsigned(dayOfMonth)).valueAt(0)`
    - bitmap也整合到string数据结构当中，整体的redis语句如`BITFIELD sign:5:202203 GET u14 0`
    - `bitField(String key, BitFieldSubCommands commands)`:执行 Redis 的 BITFIELD 命令，可以在一个字符串值上执行多个位字段子命令
    - `get(BitFieldSubCommands.BitFieldType.unsigned(dayOfMonth)).valueAt(0)`:创建一个无符号的位字段类型
      - `BitFieldType`用来设置类型，类型如下：`signed(width)`：有符号整数（可正可负）。`unsigned(width)`：无符号整数（只能是非负数）
      - `valueAt(0)`:指定位字段的偏移量（从哪个位置开始）0 表示从第 0 个 bit 开始读取
- 如果返回数字为空就直接返回。
- 如果数字存在就循环遍历
  - 让这个数字与1做与运算，得到数字的最后一个bit位
  - 如果为0，说明未签到，结束
  - 如果不为0，说明已签到，计数器+1
  - 把数字右移一位，抛弃最后一个bit位，继续下一个bit位